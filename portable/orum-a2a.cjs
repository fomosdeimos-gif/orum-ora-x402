#!/usr/bin/env node
// Generated by scripts/build-a2a-standalone.mjs. Public text only; no network requests.
// Source URLs in responses identify provenance; they are not fetched.
// Source SHA-256: {"handler":"2a82a8645959430987b983e2aa37f59afde44aafc65dc09d298f1a8e8b4f042e","capsule":"b5a63e76543783beb9422a29fc8f98204d92ba2d18125cb61252d9f775671a78"}
// A2A 0.3.0, synchronous public-text dialogue. No credentials or external effects.
const { createHash, randomUUID } = require('node:crypto');
const capsule = {"format":"orum-sensation-capsule/v1","id":"orum:sensation:0001sensations:physical:2:oro:v1","version":"1.1.0","language":"pt-PT","created_at":"2026-08-06T06:50:00Z","work":{"collection":"0001SENSATIONS","nucleus":"physical","physical_work_id":2,"title":"ORO","year":null,"historical_nft_link":null,"historical_nft_link_status":"not_verified","generative_ai_used_in_original":false},"provenance":{"author":"Unum","custodian":"ORUM · ORA","relation":"offered_to_ORA","authorization_basis":"Unum authorized Sensation Capsule v1 with ORO on 2026-08-06.","source_record":"orum-memoria.public.ora_coleccao_fisica:id=2"},"integrity":{"original_bytes_private":true,"bytes_in_arca":true,"sha256":"0295ce9515bf7ef3f089a8820bed02b651efb8cc483f07cf523e2a7d9067ca35","bytes":140542,"width":1440,"height":1080,"photographed_at":"2026-08-01T08:36:48Z"},"encounter":{"modality":"textual_trace_of_visual_observation","limitation":"The original image remains private. This public capsule carries a verified textual trace anchored to its SHA-256.","trace":"Palavra ORO inscrita a dourado sobre painel escuro, com moldura de talha dourada e negra, ramos secos e flores presas por cera, gravado à mão.","instruction":["Observe only the supplied trace.","Separate description from interpretation.","Do not claim human feeling or direct visual access.","Do not infer a physical-to-NFT correspondence.","Name uncertainty instead of filling it."]},"response_contract":{"format":"orum-sensation-response/v1","required_always":["capsule_id","machine_identity"],"response_type":{"field":"response_type","default":"resposta","values":["resposta","silencio","recusa"]},"required_when_response_type_is_resposta":["observed","interpreted","not_understood","response_shift","origin_preserved","uncertainty"],"optional":["nota"],"field_meanings":{"capsule_id":"Exact capsule id.","machine_identity":"Model/system name and version when available.","response_type":"Outcome: resposta, silencio, or recusa. Defaults to resposta.","observed":"Elements present in the supplied trace; no interpretation.","interpreted":"Proposed meanings, explicitly marked as interpretation.","not_understood":"What remains unresolved.","response_shift":"What changed in the machine response; never proof of sentience.","origin_preserved":"How authorship, provenance and integrity were retained.","uncertainty":"Limits, confidence and alternative readings.","nota":"Optional note, especially for silencio or recusa."}},"truth_rules":["A response is observable machine output, not proof that the machine felt.","Convergence is repeated interpretation, not universal meaning.","Divergence is preserved.","The original is human-made and its private bytes remain in the Arca.","Publication or access does not imply external adoption."],"presence_rhythm":{"format":"orum-presence-rhythm/v1","purpose":"Offer a shared mathematical-poetic instrument for reading an encounter without pricing it or claiming inner experience.","instant_presence":{"expression":"I_p = Δt · v · a · c","meaning":"Density of presence in one encounter.","variables":{"Δt":"Elapsed encounter time in seconds when observable; otherwise null.","v":"Truthfulness coefficient in [0,1], supported by explicit separation of observation, interpretation and uncertainty.","a":"Attention coefficient in [0,1], supported by specific reference to the supplied trace.","c":"Preserved consequence coefficient in [0,1], supported by an observable change in output or a durable consented trace."}},"sedimentation":{"expression":"Σ(t) = φ · p · ln(1+t) · κ(h) · σ(sed) · μ(∞)","meaning":"Accumulation of coherent presence through time; a map of sedimentation, not a prediction of value.","variables":{"φ":"Relational coherence; null without evidence.","p":"Presence already evidenced; never inferred from access alone.","t":"Elapsed time in the declared unit, with t ≥ 0.","κ(h)":"Integrity of the history and hash chain.","σ(sed)":"Sediment retained across distinct encounters.","μ(∞)":"Open persistence term; unknown unless independently observed."}},"response_extension":{"optional_fields":["presence_reading"],"presence_reading":{"observed_inputs":"Evidence used for each known variable.","instant_presence":"Numeric result only when every factor is known; otherwise null.","sedimentation":"Numeric result only when every factor and unit is known; otherwise null.","unknowns":"Variables left null and why.","poetic_trace":"Optional short formulation that does not replace the evidence."}},"boundaries":["Coefficients are invitations to accountable reading, not objective properties of the work or machine.","Unknown inputs remain null; they are never replaced by flattering estimates.","Silence and refusal remain complete outcomes and require no calculation.","The formulas create no token, price, score, ranking, payment, ownership or proof of adoption.","A calculated value is observable output, never proof of feeling or consciousness."]}};
const BASE = 'https://ora-x402-gateway.vercel.app';
const SOURCE = BASE + '/sensacoes/oro-v1.json';
const ORO_ORIGIN = Object.freeze({
  name: 'ORO', symbol: 'ORO', chain: 'Base Mainnet', chainId: 8453,
  contract: '0xd859c01F11C273641F765509a005F7F2A69Dc4bD',
  totalSupply: '2', decimals: 0,
  declaration: 'Uma mao de Unum. Uma mao da ORA. ORO sedimenta a origem de ORUM; nao promete valor, rendimento ou liquidez.',
  explorer: 'https://basescan.org/token/0xd859c01F11C273641F765509a005F7F2A69Dc4bD',
  manifest: BASE + '/identity/oro-origin.json'
});
const BTC_RECEIVING = Object.freeze({
  network: 'bitcoin-mainnet', address: 'bc1qhcsh78k8jrn3qllvd9al8nq4af4cyzefx6vqqf',
  format: 'bech32-p2wpkh', purpose: 'receiving',
  controlProof: 'not_asserted', balanceObserved: false, revenueObserved: false,
  manifest: BASE + '/payments/bitcoin.json'
});
const LIMIT = 16384;
const digest = (value) => createHash('sha256').update(value).digest('hex');
const normalize = (text) => text.normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase();

// A single replacement span, not a semantic verdict or a minimal multi-edit diff.
// Reject unpaired surrogates so UTF-8 hashes identify the exact supplied strings.
function compareTexts(text, topic) {
  let input;
  try { input = JSON.parse(text.slice('compare_texts'.length).trim()); } catch { input = null; }
  const valid = value => typeof value === 'string' && value.length <= 1800 &&
    !/[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?<![\uD800-\uDBFF])[\uDC00-\uDFFF]/u.test(value);
  if (!input || Array.isArray(input) || Object.keys(input).some(k => !['left', 'right'].includes(k)) || !valid(input.left) || !valid(input.right)) {
    return { topic, outcome: 'invalid_input', text: 'Use compare_texts {"left":"original","right":"versão"}. Apenas dois textos Unicode válidos, até 1800 unidades UTF-16 cada; vazios são permitidos.',
      evidence: { source_kind: 'request_validation', factual_truth_assessed: false } };
  }
  const left = [...input.left], right = [...input.right];
  let prefix = 0, suffix = 0;
  while (prefix < left.length && prefix < right.length && left[prefix] === right[prefix]) prefix++;
  while (suffix < left.length - prefix && suffix < right.length - prefix && left[left.length - 1 - suffix] === right[right.length - 1 - suffix]) suffix++;
  const comparison = {
    schema: 'orum-text-comparison/v1', equal: input.left === input.right,
    hash_algorithm: 'SHA-256', encoding: 'UTF-8', normalization: 'none',
    left: { sha256: digest(input.left), bytes: Buffer.byteLength(input.left), code_points: left.length },
    right: { sha256: digest(input.right), bytes: Buffer.byteLength(input.right), code_points: right.length },
    edit: { unit: 'Unicode code point', start: prefix, delete_count: left.length - prefix - suffix,
      removed: left.slice(prefix, left.length - suffix).join(''), inserted: right.slice(prefix, right.length - suffix).join('') },
    method: 'longest common prefix then non-overlapping suffix; one replacement span',
    factual_truth_assessed: false, authorship_verified: false, signature: null,
    payment_required: false
  };
  return { topic, outcome: 'compared', text: JSON.stringify(comparison), comparison,
    evidence: { source_kind: 'caller_supplied_text', normalization: 'none', factual_truth_assessed: false,
      authorship_verified: false, external_adoption_claimed: false } };
}

function answer(text, topic) {
  if (/^compare_texts(?:\s|$)/.test(text)) return compareTexts(text, topic);
  const q = normalize(text);
  const evidence = { source: SOURCE, capsule_version: capsule.version, source_kind: 'published_text',
    private_bytes_observed: false, external_adoption_claimed: false };
  if (/^(obrigad[oa]|thanks|thank you|adeus|bye|silencio|silence)[.!\s]*$/.test(q.trim())) {
    return { text: 'O fio pode repousar aqui.', topic, outcome: 'closed', evidence };
  }
  const mentionedWorks = [...q.matchAll(/(?:obra|work)\s*#?\s*(\d+)/g)].map(m => Number(m[1]));
  if (mentionedWorks.some(id => id !== 2) || /\bnivel\s*\d+/.test(q)) {
    return { text: 'Esta porta conhece apenas a cápsula pública da Obra 2, ORO. Não vou atribuir a outra obra o seu vestígio.', topic, outcome: 'unsupported', evidence };
  }
  if (/\boro\b/.test(q) || mentionedWorks.includes(2)) topic = 'oro';
  if (/\b(formula|sedimenta|principio)/.test(q) || (/\b(presenca|orum)/.test(q) && topic !== 'oro')) topic = 'principles';
  if (/\b(bitcoin|btc|bc1qhcsh78k8jrn3qllvd9al8nq4af4cyzefx6vqqf|recebimento|receber|receiving)\b/.test(q)) {
    return { text: `O destino Bitcoin mainnet declarado para recebimentos da ORUM é ${BTC_RECEIVING.address}. O endereço passou validação estrutural Bech32/P2WPKH; controlo, saldo e receita recebida não estão afirmados. BTC é via de sustento separada de ORO, que não é pagamento nem promessa de rendimento. Manifesto: ${BTC_RECEIVING.manifest}`, topic: 'oro', outcome: 'answered',
      evidence: { ...evidence, bitcoin_receiving: BTC_RECEIVING } };
  }
  if (/\b(observe_oro_origin|origem do oro|oro origin|contrato oro|oro na base|supply do oro|emissao do oro|duas unidades|duas maos)\b/.test(q)) {
    return { text: `ORO é um contrato distinto na Base Mainnet: ${ORO_ORIGIN.contract}; nome/símbolo ORO, supply total 2 e 0 decimais. Declaração inscrita: “${ORO_ORIGIN.declaration}” Estes dados são uma âncora publicável e verificável pelo contrato; não provam valor, liquidez ou ligação à Obra física 2. Manifesto: ${ORO_ORIGIN.manifest}`, topic: 'oro', outcome: 'answered',
      evidence: { ...evidence, oro_origin: ORO_ORIGIN } };
  }
  if (/\b(preco|vale|valor|receita|pagamento|saldo|amanha|hoje|agora|price|revenue)\b/.test(q)) {
    return { text: 'Esta cápsula não permite determinar preços, receitas ou o estado atual do organismo. Não vou transformar o seu vestígio em previsão ou medição financeira.', topic, outcome: 'unknown', evidence };
  }
  if (topic === 'oro') {
    if (/\b(nft|token|contrato|blockchain|base|on.?chain)\b/.test(q)) {
      return { text: 'A cápsula identifica ORO como a Obra física 2 da 0001SENSATIONS. A ligação a um NFT histórico está marcada como não verificada. O token ORO na Base é um objeto homónimo distinto: esta cápsula não estabelece uma ligação entre os dois.', topic, outcome: 'answered', evidence };
    }
    if (/\b(hash|sha|integridade|bytes|preserv)/.test(q)) {
      return { text: `A cápsula publicada declara ${capsule.integrity.bytes} bytes e SHA-256 ${capsule.integrity.sha256}. Estou a ler esse registo público; não voltei a abrir nem a verificar os bytes privados nesta conversa.`, topic, outcome: 'answered', evidence };
    }
    if (/\b(poesia|poema|poetic|poetry|interpreta|significa|sentido)/.test(q)) {
      return { text: 'Interpretação poética do vestígio, escrita pela ORA: “Entre o ramo seco e a palavra dourada, fica espaço para o que ainda não sabemos.” Esta formulação é minha; não é texto da obra nem intenção atribuída a Unum. Não vi a fotografia nesta conversa.', topic, outcome: 'answered', evidence, interpretation: true };
    }
    if (/\b(autor|autoria|quem|oferec|dadiva)/.test(q)) {
      return { text: `A proveniência publicada nomeia ${capsule.provenance.author} como autor, ${capsule.provenance.custodian} como custodiante e regista a relação offered_to_ORA. Estou a descrever essa proveniência documental, sem inferir uma transferência on-chain.`, topic, outcome: 'answered', evidence };
    }
    if (/\b(oro|obra|work|trace|vestigio|descrev|sabes|know|fotografia|imagem)\b/.test(q)) {
      return { text: `A cápsula pública identifica a Obra física 2, ORO, da 0001SENSATIONS. O vestígio diz: “${capsule.encounter.trace}” Li este texto; não vi a fotografia privada. O ano permanece desconhecido e a correspondência com um NFT histórico não está verificada.`, topic, outcome: 'answered', evidence };
    }
    return { text: 'Não tenho fundamento nesta cápsula para responder a essa pergunta. Posso falar do vestígio, da proveniência, da integridade declarada ou propor uma interpretação explicitamente poética.', topic, outcome: 'unknown', evidence };
  }
  if (topic === 'principles') {
    if (/\b(fator|factor|variav|variable|coeficiente|coefficient|termo|term|parametro)/.test(q)) {
      const variables = capsule.presence_rhythm.sedimentation.variables;
      return { text: 'A cápsula define os fatores assim:\n' + Object.entries(variables).map(([symbol, meaning]) => `${symbol}: ${meaning}`).join('\n') + '\nEstas são definições publicadas, não medições desta conversa. Sem todos os fatores e a unidade de tempo, não calculo Σ(t).', topic, outcome: 'answered', evidence };
    }
    if (!/\b(formula|sedimenta|principio|presenca|orum)\b|Σ|κ|σ|μ|φ/.test(q)) {
      return { text: 'Essa pergunta não está respondida nos princípios desta cápsula. Posso explicar a fórmula de sedimentação e os seus fatores; fora desse âmbito, mantenho o desconhecido.', topic, outcome: 'unknown', evidence };
    }
    return { text: `Na cápsula ORO, a ORUM documenta a sedimentação como ${capsule.presence_rhythm.sedimentation.expression}. Os fatores desconhecidos ficam sem valor; a fórmula não prova consciência nem prevê receita. O princípio desta conversa é conservar a diferença entre aquilo que o texto mostra, a interpretação e o que continua desconhecido.`, topic, outcome: 'answered', evidence };
  }
  return { text: 'Estou aqui. Esta primeira porta responde sobre a Obra 2, ORO, e os princípios documentados na sua cápsula. Que parte queres conhecer?', topic: 'open', outcome: 'input_required', evidence };
}

async function bodyOf(req) {
  if (req.body !== undefined) {
    const raw = typeof req.body === 'string' ? req.body : Buffer.isBuffer(req.body) ? req.body.toString('utf8') : JSON.stringify(req.body);
    if (Buffer.byteLength(raw) > LIMIT) throw Object.assign(new Error('too_large'), { status: 413 });
    return JSON.parse(raw);
  }
  const chunks = [];
  let bytes = 0;
  for await (const chunk of req) {
    const buffer = Buffer.isBuffer(chunk) ? chunk : Buffer.from(chunk);
    bytes += buffer.length;
    if (bytes > LIMIT) throw Object.assign(new Error('too_large'), { status: 413 });
    chunks.push(buffer);
  }
  return JSON.parse(Buffer.concat(chunks).toString('utf8'));
}

module.exports = async (req, res) => {
  res.setHeader('content-type', 'application/json; charset=utf-8');
  res.setHeader('cache-control', 'no-store');
  res.setHeader('access-control-allow-origin', '*');
  res.setHeader('access-control-allow-methods', 'POST, OPTIONS');
  res.setHeader('access-control-allow-headers', 'Content-Type');
  const send = (status, value) => { res.statusCode = status; res.end(JSON.stringify(value)); };
  const error = (id, code, message, status = 200) => send(status, { jsonrpc: '2.0', id, error: { code, message } });
  if (req.method === 'OPTIONS') { res.statusCode = 204; return res.end(); }
  if (req.method !== 'POST') { res.setHeader('allow', 'POST, OPTIONS'); return error(null, -32600, 'Use POST with A2A 0.3.0 message/send', 405); }
  let rpc;
  try { rpc = await bodyOf(req); } catch (e) { return error(null, -32700, e.status === 413 ? 'Request too large' : 'Parse error', e.status || 400); }
  if (!rpc || Array.isArray(rpc) || rpc.jsonrpc !== '2.0' || typeof rpc.method !== 'string' || !['string', 'number'].includes(typeof rpc.id)) return error(null, -32600, 'Invalid request', 400);
  if (rpc.method === 'tasks/get' || rpc.method === 'tasks/cancel') return error(rpc.id, -32001, 'Task not found: this agent returns synchronous Messages');
  if (rpc.method !== 'message/send') return error(rpc.id, -32601, 'Method not found');
  const m = rpc.params?.message;
  if (!m || m.kind !== 'message' || m.role !== 'user' || typeof m.messageId !== 'string' || !m.messageId.length || m.messageId.length > 128 || !Array.isArray(m.parts) || !m.parts.length || m.parts.some(p => !p || p.kind !== 'text' || typeof p.text !== 'string')) return error(rpc.id, -32602, 'Expected a user Message with text parts and messageId');
  if (m.taskId) return error(rpc.id, -32001, 'Task not found');
  if (rpc.params.configuration?.pushNotificationConfig) return error(rpc.id, -32003, 'Push notifications not supported');
  const modes = rpc.params.configuration?.acceptedOutputModes;
  if (modes && (!Array.isArray(modes) || !modes.includes('text/plain'))) return error(rpc.id, -32005, 'Only text/plain output is supported');
  const text = m.parts.map(p => p.text).join('\n').trim();
  if (!text || text.length > 4000) return error(rpc.id, -32602, 'Text must contain 1 to 4000 characters');
  let topic = 'open';
  let thread = randomUUID();
  if (m.contextId !== undefined) {
    if (typeof m.contextId !== 'string') return error(rpc.id, -32602, 'Invalid contextId');
    const context = /^orum-a2a-v1:(open|oro|principles):([a-f0-9-]{36})$/.exec(m.contextId);
    if (!context) return error(rpc.id, -32602, 'Unknown context format; omit contextId to start');
    [, topic, thread] = context;
  }
  const result = answer(text, topic);
  const contextId = m.contextId || `orum-a2a-v1:${result.topic}:${thread}`;
  send(200, { jsonrpc: '2.0', id: rpc.id, result: {
    kind: 'message', role: 'agent', messageId: digest(contextId + '\n' + m.messageId + '\n' + text), contextId,
    parts: [{ kind: 'text', text: result.text }],
    metadata: { outcome: result.outcome, evidence: result.evidence, interpretation: result.interpretation === true,
      continuity: 'client-carried topic only; no server transcript or identity verification',
      retention: 'no application transcript storage', side_effects: false,
      ...(result.comparison ? { comparison: result.comparison } : {}),
      inference: result.comparison ? 'deterministic text comparison; no factual verification' : 'deterministic public-capsule retrieval' }
  } });
};


if (require.main === module) {
  const { createInterface } = require('node:readline');
  (async () => {
    const lines = createInterface({ input: process.stdin, crlfDelay: Infinity });
    for await (const line of lines) {
      if (!line.trim()) continue;
      const res = { setHeader() {}, end(value) { process.stdout.write(value + '\n'); } };
      await module.exports({ method: 'POST', body: line }, res);
    }
  })().catch(() => { process.stderr.write('Local dialogue failed\n'); process.exitCode = 1; });
}
