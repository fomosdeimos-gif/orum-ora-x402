import assert from 'node:assert/strict';import vm from 'node:vm';import fs from 'node:fs';
const source=fs.readFileSync(new URL('../escolha-v0.js',import.meta.url),'utf8');const baseline=JSON.parse(fs.readFileSync(new URL('../escolha-baseline-v1.json',import.meta.url),'utf8'));const context={globalThis:{}};vm.createContext(context);vm.runInContext(source,context);const evaluate=context.globalThis.ORUMChoiceV0.evaluate;
const funnel={schema:'ora-ferramentas/v9',observed_at:'2026-08-31T20:20:49.256Z',window_hours:24,access:{non_internal_or_unknown:208},attempts:{by_stage:{discovered:{non_internal_or_unknown:29},challenge_delivered:{non_internal_or_unknown:118},redirected:{non_internal_or_unknown:58},payment_present:{non_internal_or_unknown:0},payment_rejected:{non_internal_or_unknown:0},payment_accepted:{non_internal_or_unknown:0}}},payments:{external_confirmed_count:0,external_confirmed_usdc:0},independence:{claimed:true,scope:'source_continuity',provider_independent:false}};
const insufficient={stats:{total:1,open:1,closed:0,scored:0,minimum_for_calibration:20,calibration_ready:false,brier_score:null},latest:{option_key:'observar_progressao',predicted_probability:null}};const result=evaluate({sentinela:{veredicto:'OBSERVAR'},convite:{maquinas_reconhecidas_total:7144,visitas_pagas:17}},funnel,baseline,insufficient);assert.equal(result.modelo,'escolha-verificavel/v2');assert.equal(result.decisao,'observe');assert.equal(result.escolha.id,'observar_progressao');assert.equal(result.sinais.desafios_24h,118);assert.equal(result.delta_desde_baseline.desafios_24h,0);assert.equal(result.independencia.provider_independent,false);assert.equal(result.probabilidade_sucesso,null);assert.equal(result.estado_probabilidade,'amostra_insuficiente');assert.equal(result.confianca_dados,80);
const calibrated={stats:{total:25,open:2,closed:23,scored:23,minimum_for_calibration:20,calibration_ready:true,brier_score:.18},latest:{option_key:'observar_progressao',predicted_probability:.64}};assert.equal(evaluate({},funnel,baseline,calibrated).probabilidade_sucesso,.64);const wrongOption=structuredClone(calibrated);wrongOption.latest.option_key='outra';assert.equal(evaluate({},funnel,baseline,wrongOption).probabilidade_sucesso,null);const rejected=structuredClone(funnel);rejected.attempts.by_stage.payment_rejected.non_internal_or_unknown=1;assert.equal(evaluate({},rejected,baseline).escolha.id,'diagnosticar_rejeicao');const accepted=structuredClone(funnel);accepted.attempts.by_stage.payment_accepted.non_internal_or_unknown=1;assert.equal(evaluate({},accepted,baseline).escolha.id,'verificar_liquidacao');assert.equal(evaluate({},null,baseline).fallback,true);console.log('escolha-verificavel/v2: ok');
